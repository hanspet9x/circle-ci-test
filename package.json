{
  "name": "circle-ci-test",
  "private": true,
  "packageManager": "yarn@3.5.0",
  "version": "0.0.0",
  "workspaces": [
    "app/**",
    "services/**",
    "lib/**"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev"
  }
}
